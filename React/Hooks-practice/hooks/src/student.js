function student(prop) {
  return (
    <div>
      <span>{prop.item.name}</span>
      <span>{prop.item.email}</span>
      <span>{prop.item.contact}</span>
    </div>
  );
}

export default student;
